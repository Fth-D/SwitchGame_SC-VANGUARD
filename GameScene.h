#pragma once

//======================================================================================//
//  状態																				//
//--------------------------------------------------------------------------------------//
enum GameState
{
	PLAYING,	// ゲーム進行中(通常のプレイ状態)
	GAME_OVER,	// プレイヤーが全ての残機を失った状態(敵の生成・更新を停止する)
	PAUSED,		// 一時停止中(Pauseボタンで発生、GameSceneの更新を凍結する)
	TRANSITION,	// ステージ切り替え中(次のレベルへ移行する演出)
};
extern GameState gameState;

void RefreshUIToFront();